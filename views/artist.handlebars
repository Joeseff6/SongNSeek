<section class="artist">
    <h3 class="artist-name">{{artist.name}}</h3>
    <div class="artist-details">
        {{#each artists as |album|}}
        {{> artist-details}}
        {{/each}}
    </div>
</section>